@page "/settings/experience/{UserId}"

   @* <div class="flex-row-fluid ml-lg-8">
        <div class="card card-custom">
            <div class="card-header py-3">
                <div class="card-title align-items-start flex-column">
                    <h3 class="card-label font-weight-bolder text-dark">Personal Information</h3>
                    <span class="text-muted font-weight-bold font-size-sm mt-1">Update your personal informaiton</span>
                </div>
                <div class="card-toolbar">
                </div>
            </div>
            <EditForm class="form" novalidate="novalidate" id="profile-Personal-Info" Model="@Model" OnValidSubmit="SubmitInfoAsync">
                <div class="card-body">
                    <CascadingValue Value="@FormElementStyle" Name="FormInput Class">
                        <CascadingValue Value="@InputElementStyle" Name="Input Class">
                            <CascadingValue Value="@LabelStyle" Name="Label Class">
                                <CascadingValue Value="@ElementWrapperStyle" Name="Wrapper Class">
                                    <OneUX_ImageInputBox ReturnImgbase64="Imagebase64" />
                                    <OneUX_InputSelect @bind-Value="Model.Address.Country" Label="Country" Id="country-list" Title="Select Country">
                                        @foreach (var country in CountryList)
                                        {
                                            <option value=@country>@country</option>
                                        }
                                    </OneUX_InputSelect>
                                    <OneUX_InputSelect @bind-Value="Model.Address.Country" Label="Country" Id="country-list" Title="Select Country">
                                        @foreach (var country in CountryList)
                                        {
                                            <option value=@country>@country</option>
                                        }
                                    </OneUX_InputSelect>
                                    <OneUX_DatePicker ElementId="DateOfBirth" Label="Date Of Birth" PlaceholderText="Select date" />
                                    <OneUX_DatePicker ElementId="DateOfBirth" Label="Date Of Birth" PlaceholderText="Select date" />
                                </CascadingValue>
                            </CascadingValue>
                        </CascadingValue>
                    </CascadingValue>
                    <div class="d-flex justify-content-end">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </div>
            </EditForm>
        </div>
    </div>


    @code{
        
    }*@